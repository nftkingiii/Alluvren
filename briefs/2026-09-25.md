# Alluvren — what we did today (2026-09-25)

## Baseline check
- Reviewed the takeover brief, `PROJECT_STATE.md`, `EVIDENCE.md` and `daml/DESIGN.md` against the code.
- Rebuilt the existing Daml with `dpm` (`~/dpm-sdk/windows-amd64/bin/dpm.exe`): the v1 0.1.0 DAR reproduced byte-for-byte against the recorded checksum and both existing tests passed. This closed the "Daml not freshly compiled" caveat.

## Gate 7: private investor outcomes
- Decisions: ship as an upgrade of `alluvren-v1` (0.2.0) rather than a new package; withdrawing outstanding units produces a receipt only until investor positions (Gate 2) exist.
- Finalizing a batch now creates, for each investor, a private `ClaimEntitlement` (allocated units) and `OutstandingRedemption` (unfunded units). Only that investor can see them. Investors acknowledge or withdraw once and receive demo receipts; no asset moves.
- The existing finalize choice and BitSafe proposal kept their signatures, so the governed path is unchanged.

## Local proof
- Upgrade check against the deployed 0.1.0 DAR passes; a deliberately breaking change was rejected, proving the check runs.
- 7/7 Daml Script tests pass: conservation (600/300/100 against 500 → 300/150/50), cross-investor privacy, wrong-party actions, once-only acknowledgment and withdrawal, zero allocation, and no investor records after a rejected finalization.
- A deliberate privacy leak made the tests fail, proving they catch it.

## LocalNet proof
- Started the VM and recovered LocalNet without resetting data; stopped it afterwards and verified `TERMINATED`.
- Distributed 0.2.0 to all three participants; each reports package `070bfd7a…363b24`.
- Run `gate7-1790341203`: a BitSafe-governed finalization created the investor records. Each investor saw only its own; operator, reviewers and proposer saw none. Wrong-party and repeat actions were rejected. Cleanup verified.
- New scripts: `infra/distribute-dar-localnet.sh` and `infra/test-gate7-localnet.sh` (with a `cleanup` mode for interrupted runs).

## Commits on `main`
- `71742a7` Gate 7 Daml layer
- `6b71556` LocalNet verification and scripts
- `2c4b7ff` Keep shell scripts on LF line endings

## Next slice designed: policy-driven role governance
Grounded in the Maya/Daniel/Priya interview notes, which name five parties with a say (COO, Operations, Treasury, Compliance, Administrator):
- On-ledger fund policy with N-of-M quorums per role; batches pin the exact policy version.
- Separation of duties: approvers cannot be batch investors, hold two roles, or be the proposer/operator.
- Policy changes require the BitSafe threshold and block in-flight batches.
- Batch-dependent rules, e.g. a batch funded below 50% requires Compliance.
Spec and tests P-01..P-10 are in `daml/DESIGN.md`.

## Open items
- Separate-login visibility test per investor (needs per-investor ledger users/tokens).
- DecMan audit endpoint only returns entries up to offset 876, so it doesn't show today's runs.
- Gate 8: real sign-in mapped to parties before any live writes.
- Confirm the proposed role policy with the interview contacts.
- Recheck the submission deadline (last recorded: 2026-10-09 23:59 UTC).
